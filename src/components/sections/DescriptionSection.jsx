import SectionTitle from "../ui/SectionTitle";
import { container, sectionPadding } from "../../styles/theme";

export default function DescriptionSection() {
    return (
        <section id="desc" className={sectionPadding}>
            <div className={container}>

            </div>
        </section>
    );
}